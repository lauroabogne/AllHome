<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/tabLayoutBgColor" /> <!-- Background color, adjust as needed -->
    <corners android:radius="0dp" /> <!-- Adjust corner radius if needed -->
    <stroke
        android:width="0dp"
        android:color="@android:color/transparent" /> <!-- No stroke -->
</shape>